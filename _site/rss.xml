<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.2">Jekyll</generator><link href="http://localhost:4000/rss.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-07-25T05:45:39+01:00</updated><id>http://localhost:4000/rss.xml</id><title type="html">DEVBLOG BIOS UTILITY</title><subtitle>Dru Delarosa - Devblog</subtitle><entry><title type="html">slurm-riscv</title><link href="http://localhost:4000/2026/07/22/slurm-riscv.html" rel="alternate" type="text/html" title="slurm-riscv" /><published>2026-07-22T05:58:17+01:00</published><updated>2026-07-22T05:58:17+01:00</updated><id>http://localhost:4000/2026/07/22/slurm-riscv</id><content type="html" xml:base="http://localhost:4000/2026/07/22/slurm-riscv.html"><![CDATA[<h1 id="slurm-on-risc-v">slurm on risc-v</h1>

<p>venturing into running slurm and openmpi on my risc-v microcluster. 
this idea came to fruition simply through pure curiosity; i have already created a fully functioning risc-v cluster with:</p>

<ul>
  <li>kubernetes</li>
  <li>docker</li>
  <li>local container registry</li>
  <li>required toolchains</li>
  <li>prometheus</li>
  <li>victoriametrics</li>
  <li>grafana</li>
</ul>

<p>essentially a datacenter in microformat.</p>

<p>however i want to elevate this to not only micro-datacenter, but to micro-supercomputer and hpc environment by installing and running Slurm and openMPI.</p>

<h2 id="why-slurm">why slurm?</h2>
<p>because while setting up a core node and x amount of nodes with kubernetes is essentially a cluster;
slurm makes it a true cluster by assigning and sharing resources across each node.
kubernetes merely does this with containers and while it does allocate resources it does not share them.
 slurm is the compute cluster, sharing cpu and resources while making the system feel like a symbiotic machine rather than a load of boards stuck together.</p>

<h2 id="is-risc-v-going-to-be-a-problem">is risc-v going to be a problem?</h2>
<p>yes it is but that is to be expected; this is a rather niche area ive stumbled into and even more so because it’s hpc + risc-v. the only real issue i came across was mpi and slurm not working together properly but this was solved by building slurm 24 from source, linking it to openmpi + pmix and using mpirun instead of slurms srun as risc-v integration isnt quite there yet. but it still works.</p>

<h2 id="installing-pmix">installing pmix</h2>

<pre><code class="language-bash">wget https://github.com/openpmix/openpmix/releases/download/v4.2.9/pmix-4.2.9.tar.gz
tar xf pmix-4.2.9.tar.gz
cd pmix-4.2.9
./configure --prefix=/usr/local/pmix
make -j$(nproc)
sudo make install
</code></pre>

<h2 id="adding-to-path">adding to path</h2>

<pre><code class="language-bash">echo 'export PMIX_INSTALL_PREFIX=/usr/local/pmix'           | sudo tee /etc/profile.d/pmix.sh
echo 'export LD_LIBRARY_PATH=/usr/local/pmix/lib:$LD_LIBRARY_PATH' | sudo tee -a /etc/profile.d/pmix.sh
echo 'export PATH=/usr/local/pmix/bin:$PATH'                | sudo tee -a /etc/profile.d/pmix.sh
source /etc/profile.d/pmix.sh
</code></pre>

<h2 id="install-slurm-pre-requisites">install slurm pre-requisites</h2>

<pre><code class="language-bash">sudo apt install libhwloc-dev libssl-dev libreadline-dev libpam0g-dev libmunge-dev munge libjson-c-dev libz-dev
</code></pre>

<h2 id="build--install-slurm">build &amp; install slurm</h2>

<pre><code class="language-bash">wget https://download.schedmd.com/slurm/slurm-24.05.3.tar.bz2
tar xf slurm-24.05.3.tar.bz2
cd slurm-24.05.3
./configure --prefix=/usr/local/slurm
    --sysconfdir=/etc/slurm 
    --with-pmix=/usr/local/pmix 
    --with-munge
    --enable-pam
make -j$(nproc)
sudo make install
</code></pre>

<h2 id="adding-slurm-to-path">adding slurm to path</h2>

<pre><code class="language-bash">echo 'export PATH=/usr/local/slurm/bin:/usr/local/slurm/sbin:$PATH' | sudo tee /etc/profile.d/slurm.sh
source /etc/profile.d/slurm.sh
</code></pre>

<h2 id="build--install-openmpi">build &amp; install openmpi</h2>

<p>here, we are limited to &lt; 4.1.x as we need c++ bindings. these are deprecated in version 5x+</p>
<pre><code class="language-bash">wget https://download.open-mpi.org/release/open-mpi/v4.1/openmpi-4.1.6.tar.bz2
tar xf openmpi-4.1.6.tar.bz2
cd openmpi-4.1.6

./configure --prefix=/usr/local/openmpi
    --with-pmix=/usr/local/pmix 
    --with-slurm 
    --enable-mpi-cxx
make -j$(nproc)
sudo make install
</code></pre>

<h2 id="create-a-slurm-control-daemon-service">create a slurm control daemon service</h2>

<p>create this service on the core/controller node only.</p>

<pre><code class="language-bash">sudo vim /etc/systemd/system/slurmctld.service
</code></pre>
<p>unit file:</p>

<pre><code class="language-bash">[Unit]
Description=Slurm controller daemon
After=network.target munge.service
ConditionPathExists=/etc/slurm/slurm.conf

[Service]
Type=simple
User=slurm
Group=slurm
ExecStart=/usr/local/slurm/sbin/slurmctld -D
ExecReload=/bin/kill -HUP $MAINPID
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target

</code></pre>

<h2 id="create-slurm-daemon-service">create slurm daemon service</h2>

<p>create this on the core/controller node; then copy it via scp to any worker nodes.</p>

<pre><code class="language-bash">sudo vim /etc/systemd/system/slurmd.service
</code></pre>

<p>unit file:</p>

<pre><code class="language-bash">[Unit]
Description=Slurm node daemon
After=network.target munge.service
ConditionPathExists=/etc/slurm/slurm.conf

[Service]
Type=simple
User=root
ExecStart=/usr/local/slurm/sbin/slurmd -D
ExecReload=/bin/kill -HUP $MAINPID
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
</code></pre>

<h2 id="enable-the-unit-files">enable the unit files</h2>

<p>on the core:</p>

<pre><code class="language-bash">sudo systemctl enable slurmctld
sudo systemctl enable slurmd
sudo systemctl start slurmctld
sudo systemctl start slurmd
</code></pre>

<p>remaining nodes:</p>

<pre><code class="language-bash">sudo systemctl enable slurmd
sudo systemctl start slurmd
</code></pre>

<h2 id="enable-munge">enable munge</h2>

<p>on all nodes:</p>

<pre><code class="language-bash">sudo systemctl enable munge
sudo systemctl start munge
</code></pre>

<h2 id="test-slurm">test slurm</h2>

<p>on the core:</p>

<pre><code class="language-bash">sinfo
</code></pre>

<p>all the nodes should be listed as IDLE
&amp; running our test program proves it:</p>

<pre><code class="language-bash">mpirun -np 3 --host riscv-core,riscv-node-1,riscv-node-2 ./hello
</code></pre>
<p>as we recieve a hello from rank 0 of 3 on each node.</p>

<p>now we have slurm running entirely on risc-v!</p>]]></content><author><name></name></author><category term="riscv" /><category term="linux" /><category term="hpc" /><category term="slurm" /><summary type="html"><![CDATA[slurm on risc-v]]></summary></entry><entry><title type="html">riscv-microcluster</title><link href="http://localhost:4000/2026/07/22/riscv-microcluster.html" rel="alternate" type="text/html" title="riscv-microcluster" /><published>2026-07-22T05:48:04+01:00</published><updated>2026-07-22T05:48:04+01:00</updated><id>http://localhost:4000/2026/07/22/riscv-microcluster</id><content type="html" xml:base="http://localhost:4000/2026/07/22/riscv-microcluster.html"><![CDATA[<h1 id="risc-v-distributed-compute-cluster---notes-from-a-small-intentional-datacenter">risc-v distributed compute cluster - notes from a small, intentional datacenter</h1>

<p>for the past few months ive been building a microcluster/mini datacenter at home. however this isn’t your usual cluster or server build. it’s entirely in risc-v.</p>

<p>i wanted to build this for a few reasons:</p>

<p>to challenge myself, to experience risc-v on native hardware, experimentation, rust &amp; risc-v development, to further my knowledge of clustered environments, kubernetes &amp; docker.</p>

<p>this post is a short overview of the cluster, the design choices behind it, and what I learned from running real workloads on non‑x86 hardware.</p>

<p>the cluster consists of:</p>

<ul>
  <li>3× orange pi rv2 (8gb ram)</li>
  <li>nvme storage (2x nvme slots per node)</li>
  <li>dual gbe networking (2x gbe nic per node)</li>
  <li>poe power via a tp-link switch</li>
  <li>custom microrack built from a modified phanteks hdd cage</li>
  <li>5v usb-c poe splitters powering each node</li>
</ul>

<p>each node sits on its own tray, making the whole system feel like a miniature datacenter: modular, accessible, and intentionally designed.</p>

<h2 id="networking--topology">networking &amp; topology</h2>

<p>the cluster uses:</p>

<ul>
  <li>multi‑nic networking</li>
  <li>per‑node static addressing</li>
  <li>clean, predictable dns</li>
  <li>local registry for architecture‑specific container builds</li>
  <li>dedicated management network for ssh, metrics, and orchestration</li>
</ul>

<p>the goal here wasn’t speed, more cohesion and i wanted a cluster that behaved like a single organism, not three boards taped together.</p>

<h2 id="software-stack">software stack</h2>

<p>every node runs a minimal ubuntu server linux environment (not my ideal choice; but its what the board supports right now) with:</p>

<ul>
  <li>kubernetes (recompiled for RISC‑V)</li>
  <li>prometheus + victoriametrics</li>
  <li>grafana</li>
  <li>private container registry &amp; custom certificates (CA/SAN)</li>
  <li>rust‑based microservices</li>
  <li>custom NVMe metrics exporter i wrote in rust for testing i/o behaviour and because this kernel doesn’t expose nvme temps natively</li>
</ul>

<p>entire stack is built from source where possible because risc-v support is still uneven and reproducibility matters</p>

<p>one of the main goals of this cluster was to explore:</p>

<ul>
  <li>cross‑compilation</li>
  <li>architecture‑specific debugging</li>
  <li>reproducible builds</li>
  <li>behaviour differences between risc-v, arm, and x86</li>
  <li>how distributed workloads behave on low‑power hardware</li>
</ul>

<p>to test the cluster, i wrote several small Rust microservices:</p>

<ul>
  <li>simple message‑passing workloads</li>
  <li>storage‑heavy tasks</li>
  <li>cpu‑bound tasks</li>
  <li>mixed workloads to test scheduling behaviour</li>
</ul>

<p>also built a custom nvme exporter to measure:</p>

<ul>
  <li>latency</li>
  <li>throughput</li>
  <li>queue depth</li>
  <li>thermal behaviour</li>
  <li>consistency under load</li>
</ul>

<p>this gave me a real sense of how risc-v behaves under distributed pressure.. not theoretical benchmarks, but actual system behaviour.</p>

<p>i care a lot about observability, so the cluster exposes:</p>

<ul>
  <li>cpu</li>
  <li>memory</li>
  <li>network</li>
  <li>nvme</li>
  <li>container metrics</li>
  <li>orchestration metrics</li>
  <li>custom Rust exporter data</li>
</ul>

<p>grafana dashboards reflect the living state of the cluster.</p>

<h2 id="what-i-learned">what i learned</h2>

<ul>
  <li>risc-v is absolutely viable for real distributed workloads, not just hobby boards.</li>
  <li>toolchain consistency matters more than raw performance.</li>
  <li>kubernetes on RISC‑V works, but you need to build it yourself.</li>
  <li>small clusters teach you more than big ones because every detail is visible.</li>
</ul>

<p>this project wasn’t about building something powerful, massive ram numbers, a huge amount of storage or raw cpu power, it was about building something honest, cohesive, and expressive; a system that reflects how i think about engineering.</p>]]></content><author><name></name></author><category term="riscv" /><category term="linux" /><category term="hpc" /><category term="orangepi" /><summary type="html"><![CDATA[risc-v distributed compute cluster - notes from a small, intentional datacenter]]></summary></entry></feed>